@model Manage_KPI_or_OKR_System.Models.Warehouse
@{
    ViewData["Title"] = "Thêm Kho hàng";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid px-4 mt-4">
    <div class="row mb-3">
        <div class="col-12">
            <h3 class="text-dark fw-bold">
                <i class="bi bi-house-door-fill me-2"></i>Thêm Kho hàng Mới
            </h3>
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb bg-transparent p-0 m-0">
                    <li class="breadcrumb-item"><a href="/" class="text-dark text-decoration-none opacity-75">Trang chủ</a></li>
                    <li class="breadcrumb-item"><a asp-action="Index" class="text-dark text-decoration-none opacity-75">Quản lý Kho</a></li>
                    <li class="breadcrumb-item active text-dark fw-bold" aria-current="page">Thêm mới</li>
                </ol>
            </nav>
        </div>
    </div>

    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="content-card rounded-3">
                <div class="card-header-custom bg-white border-bottom py-3">
                    <h5 class="mb-0 fw-bold text-dark"><i class="bi bi-geo-alt me-2"></i>Thông tin Kho hàng</h5>
                </div>
                <div class="card-body-custom p-4">
                    <form asp-action="Create" method="post">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                        <div class="mb-3">
                            <label asp-for="WarehouseCode" class="form-label fw-bold text-dark">Mã kho <span class="text-danger">*</span></label>
                            <input asp-for="WarehouseCode" class="form-control" placeholder="VD: KHO-HN, WH-MAIN..." required />
                            <span asp-validation-for="WarehouseCode" class="text-danger small"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="WarehouseName" class="form-label fw-bold text-dark">Tên kho hàng <span class="text-danger">*</span></label>
                            <input asp-for="WarehouseName" class="form-control" placeholder="Nhập tên kho hàng" required />
                            <span asp-validation-for="WarehouseName" class="text-danger small"></span>
                        </div>

                        <div class="mb-4">
                            <label asp-for="Address" class="form-label fw-bold text-dark">Địa chỉ / Vị trí</label>
                            <textarea asp-for="Address" class="form-control" rows="2" placeholder="Nhập địa chỉ của kho hàng"></textarea>
                            <span asp-validation-for="Address" class="text-danger small"></span>
                        </div>

                        <div class="d-flex justify-content-end gap-2 pt-3 border-top">
                            <a asp-action="Index" class="btn-outline-custom px-4">Hủy bỏ</a>
                            <button type="submit" class="btn-primary-custom px-4">
                                <i class="bi bi-save me-1"></i> Lưu Kho hàng
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
