# Codex implementation plan

- Task ID: `<task-id>`
- Iteration: `1`
- Maximum iterations: `5`
- Created by: `Codex`
- Executor: `AGY IDE / codex-executor`

## Goal

`<Exact outcome>`

## User request

`<Original intent in concise form>`

## Pre-existing working-tree changes to preserve

```text
<git status --short before AGY starts>
```

## In scope

- `<Files, modules, and behavior AGY may change>`

## Out of scope

- `<Explicit exclusions>`

## Implementation steps

1. `<Decision-complete step>`

## Acceptance criteria

1. `<Observable criterion>`

## Required verification

```powershell
<Exact commands AGY must run>
```

## Safety and authorization

- Preserve unrelated and pre-existing changes.
- Do not commit, push, deploy, publish, change accounts, or perform destructive operations.
- `<Any task-specific authorization or prohibition>`

## Required handoff

Write `report.json` matching `.agents/orchestration/report.schema.json`, update `status.json`, and stop for Codex review.
